/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true,
  }
//   images: {
//     domains: ['infura-ipfs.io'],
//  }
}

module.exports = nextConfig
